<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>com.example</groupId>
  <artifactId>payments-service-notests</artifactId>
  <version>1.0.0</version>
  <packaging>jar</packaging>
  <name>payments-service-notests</name>
  <description>
    Sibling of payments-service with no unit/IT sources under src/test/java.
    Used to demo upgrade-delta's REACHABILITY_ONLY honesty path (grade from
    call-site analysis; Surefire skipped). Not a production payments product.
    Lightwell versions (when used) come only from the public console demos:
    java-remediated-demo / java-validated-demo (public-lightwell-demo Maven feeds).
  </description>

  <properties>
    <java.version>17</java.version>
    <maven.compiler.release>17</maven.compiler.release>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <!-- Framework: community builds (not on the public Lightwell demo feeds).
         spring-core has its own property so Lightwell can remediate it without
         also bumping spring-web/webmvc (those rhlw jars are not on the public
         demo feeds). -->
    <spring-boot.version>2.7.18</spring-boot.version>
    <spring.version>5.3.18</spring.version>
    <spring-core.version>5.3.18</spring-core.version>
    <spring-security.version>5.7.11</spring-security.version>
    <!-- Demo remediations stay on community so the badge can show available updates.
         fromVersions match console.redhat.com Lightwell public demos. -->
    <jackson.version>2.13.4</jackson.version>
    <commons-io.version>2.11.0</commons-io.version>
    <httpclient.version>4.5.12</httpclient.version>
    <json-path.version>2.7.0</json-path.version>
    <!-- Deliberately pre-1.33 community so ConfigLoader's removed constructor stays reachable. -->
    <snakeyaml.version>1.30</snakeyaml.version>
  </properties>

  <repositories>
    <!-- Public Lightwell demo feeds only (unauthenticated). Maven Central is implicit.
         https://console.redhat.com/lightwell/demo/java-remediated-demo
         https://console.redhat.com/lightwell/demo/java-validated-demo -->
    <repository>
      <id>lightwell-public-remediated</id>
      <url>https://packages.redhat.com/lightwell/public-lightwell-demo/java/remediated/</url>
      <releases><enabled>true</enabled></releases>
      <snapshots><enabled>false</enabled></snapshots>
    </repository>
    <repository>
      <id>lightwell-public-validated</id>
      <url>https://packages.redhat.com/lightwell/public-lightwell-demo/java/validated/</url>
      <releases><enabled>true</enabled></releases>
      <snapshots><enabled>false</enabled></snapshots>
    </repository>
  </repositories>

  <dependencies>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot</artifactId>
      <version>${spring-boot.version}</version>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-autoconfigure</artifactId>
      <version>${spring-boot.version}</version>
    </dependency>
    <dependency>
      <groupId>org.springframework</groupId>
      <artifactId>spring-web</artifactId>
      <version>${spring.version}</version>
    </dependency>
    <dependency>
      <groupId>org.springframework</groupId>
      <artifactId>spring-webmvc</artifactId>
      <version>${spring.version}</version>
    </dependency>
    <dependency>
      <groupId>org.springframework</groupId>
      <artifactId>spring-core</artifactId>
      <version>${spring-core.version}</version>
    </dependency>
    <dependency>
      <groupId>org.springframework.security</groupId>
      <artifactId>spring-security-core</artifactId>
      <version>${spring-security.version}</version>
    </dependency>
    <dependency>
      <groupId>org.springframework.security</groupId>
      <artifactId>spring-security-web</artifactId>
      <version>${spring-security.version}</version>
    </dependency>

    <dependency>
      <groupId>com.fasterxml.jackson.core</groupId>
      <artifactId>jackson-databind</artifactId>
      <version>${jackson.version}</version>
    </dependency>
    <dependency>
      <groupId>commons-io</groupId>
      <artifactId>commons-io</artifactId>
      <version>${commons-io.version}</version>
    </dependency>
    <dependency>
      <groupId>org.apache.httpcomponents</groupId>
      <artifactId>httpclient</artifactId>
      <version>${httpclient.version}</version>
    </dependency>
    <dependency>
      <groupId>com.jayway.jsonpath</groupId>
      <artifactId>json-path</artifactId>
      <version>${json-path.version}</version>
    </dependency>
    <dependency>
      <groupId>org.yaml</groupId>
      <artifactId>snakeyaml</artifactId>
      <version>${snakeyaml.version}</version>
    </dependency>

    <dependency>
      <groupId>org.junit.jupiter</groupId>
      <artifactId>junit-jupiter</artifactId>
      <version>5.9.3</version>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <finalName>payments-service</finalName>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <version>3.11.0</version>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-surefire-plugin</artifactId>
        <version>3.1.2</version>
        <!-- Live pipeline passes -Dsurefire.includesFile=... from upgrade-delta router. -->
        <configuration>
          <failIfNoSpecifiedTests>false</failIfNoSpecifiedTests>
        </configuration>
      </plugin>
      <!-- Fat / uber jar so upgrade-delta fingerprints deps inside the artifact
           (avoids declared-not-shipped noise from thin jars). -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-shade-plugin</artifactId>
        <version>3.5.1</version>
        <executions>
          <execution>
            <phase>package</phase>
            <goals><goal>shade</goal></goals>
            <configuration>
              <createDependencyReducedPom>false</createDependencyReducedPom>
              <transformers>
                <transformer implementation="org.apache.maven.plugins.shade.resource.ManifestResourceTransformer">
                  <mainClass>com.example.payments.PaymentsApplication</mainClass>
                </transformer>
                <transformer implementation="org.apache.maven.plugins.shade.resource.ServicesResourceTransformer"/>
              </transformers>
              <filters>
                <filter>
                  <artifact>*:*</artifact>
                  <excludes>
                    <exclude>META-INF/*.SF</exclude>
                    <exclude>META-INF/*.DSA</exclude>
                    <exclude>META-INF/*.RSA</exclude>
                  </excludes>
                </filter>
              </filters>
            </configuration>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>org.cyclonedx</groupId>
        <artifactId>cyclonedx-maven-plugin</artifactId>
        <version>2.7.11</version>
        <executions>
          <execution>
            <phase>package</phase>
            <goals><goal>makeAggregateBom</goal></goals>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>org.jacoco</groupId>
        <artifactId>jacoco-maven-plugin</artifactId>
        <version>0.8.12</version>
        <executions>
          <execution><goals><goal>prepare-agent</goal></goals></execution>
          <execution><id>report</id><phase>test</phase><goals><goal>report</goal></goals></execution>
        </executions>
      </plugin>
    </plugins>
  </build>

  <!-- Kept for existing Tekton/CI invocations of -Pci-community (same pins as default). -->
  <profiles>
    <profile>
      <id>ci-community</id>
      <properties>
        <spring-boot.version>2.7.18</spring-boot.version>
        <spring.version>5.3.18</spring.version>
        <spring-core.version>5.3.18</spring-core.version>
        <spring-security.version>5.7.11</spring-security.version>
        <jackson.version>2.13.4</jackson.version>
        <commons-io.version>2.11.0</commons-io.version>
        <httpclient.version>4.5.12</httpclient.version>
        <json-path.version>2.7.0</json-path.version>
        <snakeyaml.version>1.30</snakeyaml.version>
      </properties>
    </profile>
  </profiles>
</project>
